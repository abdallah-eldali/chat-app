import React from "react"

//When a newly user joins to the chat,
//then recieve all the prior messages
//and create an array of components
function getAllMessages(props){

}

//props is an array of messages
function MessagesBox(props){
    return (
        <div>
            <ul>
                
            </ul>
        </div>
    );
}

export default MessagesBox